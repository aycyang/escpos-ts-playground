{
  "name": "escpos-ts-playground",
  "version": "0.0.1",
  "description": "",
  "main": "src/main.ts",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "serve": "npx http-server public",
    "build": "npx esbuild src/main.ts --bundle --outfile=public/main.js --target=es2022"
  },
  "author": "",
  "license": "BSD-3-Clause",
  "devDependencies": {
    "esbuild": "^0.27.0"
  },
  "dependencies": {
    "escpos-ts": "file:../escpos-ts",
    "http-server": "^14.1.1"
  }
}
