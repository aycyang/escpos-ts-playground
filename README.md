# escpos-ts-playground

### TODO

- [ ] consider making parsing async and cancelable, e.g. a generator function that checks if it should stop after parsing every command
- [ ] once escpos-ts is available on npm registry, source it from there
